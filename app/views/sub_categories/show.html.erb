<%= render 'layouts/sidebar', categories: @main_categories, this_category: @sub_category.main_category %>

<div class="content">
	<h1><%= @sub_category.name %></h1>
	<%= render_breadcrumbs %>
	<div class="products">
		<% @sub_category.products.each do |product| %>
			<%= render 'products/product_thumbnail', product: product %>
		<% end %>
	</div>
</div>